{
  "title": "Approval Auditor",
  "description": "Scan and identify dangerous ERC-20 and NFT token approvals for any wallet \u2014 flags unlimited approvals, high-risk spenders, and total value at risk across all active approvals.",
  "category": "security",
  "url": "https://goldrush.dev/docs/api/security/get-approvals/",
  "tag": "Free Tier",
  "date-added": "2026-03-12",
  "slug": "approval-auditor",
  "complexity": "Beginner",
  "useCase": "Build a wallet security scanner that shows all active token approvals, flags the dangerous ones, and calculates the total USD value at risk from unlimited approvals.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei"
  ],
  "creditCost": "1 credit/call",
  "endpoints": [
    "GET /v1/{chainName}/approvals/{walletAddress}/"
  ],
  "keyParams": {
    "chainName": "EVM chain identifier (e.g. eth-mainnet, matic-mainnet)",
    "walletAddress": "0x wallet address to audit"
  },
  "responseFields": [
    "items[].token_address",
    "items[].token_ticker_symbol",
    "items[].spender_address",
    "items[].spender_name",
    "items[].value_at_risk",
    "items[].risk_factor"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nasync function auditApprovals(chainName: string, walletAddress: string) {\n  const resp = await client.SecurityService.getApprovals(\n    chainName,\n    walletAddress\n  );\n\n  if (resp.error) throw new Error(resp.error_message);\n\n  const approvals = resp.data.items;\n  const totalAtRisk = approvals.reduce((sum, a) => sum + (a.value_at_risk ?? 0), 0);\n  const highRisk = approvals.filter(a => (a.risk_factor ?? 0) > 0.7);\n\n  return {\n    totalApprovals: approvals.length,\n    totalValueAtRisk: totalAtRisk,\n    highRiskCount: highRisk.length,\n    approvals: approvals.map(a => ({\n      token: a.token_ticker_symbol,\n      spender: a.spender_name ?? a.spender_address,\n      valueAtRisk: a.value_at_risk,\n      risk: a.risk_factor,\n    })),\n  };\n}",
  "buildsWith": [
    "token-approval-scanner",
    "token-approval-risk-scanner",
    "goldrush-kit-approvals"
  ]
}