{
  "title": "Agent Wallet Skill",
  "description": "Give AI agents the ability to read wallet state, balances, and transaction data.",
  "category": "agents",
  "url": "https://goldrush.dev",
  "tag": "Open Source",
  "date-added": "2026-03-12",
  "slug": "agent-wallet-skill",
  "complexity": "Beginner",
  "useCase": "Equip a Claude or GPT agent with the ability to answer questions like 'What tokens does this wallet hold?' and 'What did this wallet do recently?' using live on-chain data.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei"
  ],
  "creditCost": "1 credit/call",
  "endpoints": [
    "GET /v1/{chainName}/address/{walletAddress}/balances_v2/",
    "GET /v1/{chainName}/address/{walletAddress}/transactions_v3/"
  ],
  "keyParams": {
    "chainName": "EVM chain identifier (e.g. eth-mainnet, base-mainnet)",
    "walletAddress": "0x wallet address the agent is querying"
  },
  "responseFields": [
    "items[].contract_name",
    "items[].contract_ticker_symbol",
    "items[].balance",
    "items[].quote",
    "items[].tx_hash",
    "items[].block_signed_at",
    "items[].successful"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\n// Composable wallet skill for AI agents\nexport const walletSkill = {\n  name: 'get_wallet_data',\n  description: 'Fetch token balances and recent transactions for a wallet address',\n  async execute(chainName: string, walletAddress: string) {\n    const [balResp, txResp] = await Promise.all([\n      client.BalanceService.getTokenBalancesForWalletAddress(chainName, walletAddress),\n      client.TransactionService.getTransactionsForAddressV3(chainName, walletAddress, 0),\n    ]);\n\n    if (balResp.error) throw new Error(balResp.error_message);\n    if (txResp.error) throw new Error(txResp.error_message);\n\n    const totalUsd = balResp.data.items.reduce((s, t) => s + (t.quote ?? 0), 0);\n    return {\n      totalUsd,\n      topTokens: balResp.data.items.slice(0, 5).map(t => t.contract_ticker_symbol),\n      recentTxCount: txResp.data.items.length,\n    };\n  },\n};",
  "buildsWith": [
    "claude-goldrush-skill",
    "goldrush-mcp-server",
    "agent-fleet-monitor"
  ]
}