{
  "title": "Token Approval Scanner",
  "description": "Identify and audit all ERC-20 and NFT token approvals granted by any wallet address \u2014 see spender name, value at risk, and risk factor for each active approval.",
  "category": "token",
  "url": "https://goldrush.dev/docs/api/security/get-approvals/",
  "tag": "Free Tier",
  "date-added": "2026-03-12",
  "slug": "token-approval-scanner",
  "complexity": "Beginner",
  "useCase": "Build a token approval checker that shows every active approval a wallet has granted \u2014 highlight unlimited approvals and flag spenders by risk level.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei"
  ],
  "creditCost": "1 credit/call",
  "endpoints": [
    "GET /v1/{chainName}/approvals/{walletAddress}/"
  ],
  "keyParams": {
    "chainName": "EVM chain identifier (e.g. eth-mainnet, matic-mainnet)",
    "walletAddress": "0x wallet address to scan approvals for"
  },
  "responseFields": [
    "items[].token_address",
    "items[].token_ticker_symbol",
    "items[].spender_address",
    "items[].spender_name",
    "items[].value_at_risk",
    "items[].risk_factor"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nasync function scanTokenApprovals(chainName: string, walletAddress: string) {\n  const resp = await client.SecurityService.getApprovals(\n    chainName,\n    walletAddress\n  );\n\n  if (resp.error) throw new Error(resp.error_message);\n\n  return resp.data.items.map(a => ({\n    token: a.token_ticker_symbol,\n    tokenAddress: a.token_address,\n    spender: a.spender_name ?? a.spender_address,\n    valueAtRisk: a.value_at_risk,\n    riskFactor: a.risk_factor,\n    isHighRisk: (a.risk_factor ?? 0) > 0.7,\n  }));\n}",
  "buildsWith": [
    "token-approval-risk-scanner",
    "approval-auditor",
    "goldrush-kit-approvals"
  ]
}