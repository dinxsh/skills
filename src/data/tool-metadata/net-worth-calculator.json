{
  "title": "Net Worth Calculator",
  "description": "Calculate total wallet net worth across all EVM and non-EVM chains instantly.",
  "category": "wallet",
  "url": "https://goldrush.dev",
  "tag": "Free Tier",
  "date-added": "2026-03-12",
  "slug": "net-worth-calculator",
  "complexity": "Beginner",
  "useCase": "Show a user their total crypto net worth in USD across every chain they hold assets on — the one-call portfolio summary widget.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei"
  ],
  "creditCost": "1 credit/call",
  "endpoints": [
    "GET /v1/address/{walletAddress}/balances/"
  ],
  "keyParams": {
    "walletAddress": "0x wallet address to aggregate balances across all chains"
  },
  "responseFields": [
    "items[].chain_name",
    "items[].items[].contract_name",
    "items[].items[].balance",
    "items[].items[].quote"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nasync function getNetWorth(walletAddress: string) {\n  const resp = await client.BalanceService.getTokenBalances(walletAddress);\n\n  if (resp.error) throw new Error(resp.error_message);\n\n  let totalUsd = 0;\n  const byChain: Record<string, number> = {};\n\n  for (const chain of resp.data.items) {\n    const chainTotal = chain.items.reduce((sum, t) => sum + (t.quote ?? 0), 0);\n    byChain[chain.chain_name] = chainTotal;\n    totalUsd += chainTotal;\n  }\n\n  return { totalUsd, byChain };\n}",
  "buildsWith": [
    "multichain-portfolio",
    "wallet-balances-skill",
    "address-activity-scanner"
  ]
}