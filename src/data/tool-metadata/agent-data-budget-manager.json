{
  "title": "Agent Data Budget Manager",
  "description": "Track and optimise GoldRush API credit consumption across AI agent workflows \u2014 monitor chain sync status to ensure data freshness before running expensive multi-chain queries.",
  "category": "agents",
  "url": "https://goldrush.dev/docs/api/base/get-all-chain-statuses/",
  "tag": "Open Source",
  "date-added": "2026-03-16",
  "slug": "agent-data-budget-manager",
  "complexity": "Intermediate",
  "useCase": "Build a credit-aware AI agent wrapper that checks chain sync status before issuing queries and tracks cumulative API credit spend across a multi-step workflow.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei"
  ],
  "creditCost": "0.1 credit/call",
  "endpoints": [
    "GET /v1/chains/status/"
  ],
  "keyParams": {},
  "responseFields": [
    "items[].name",
    "items[].synced_block_height",
    "items[].synced_blocked_signed_at"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nlet totalCreditsSpent = 0;\n\nasync function checkChainFreshness(chainName: string) {\n  const resp = await client.BaseService.getAllChainStatus();\n  totalCreditsSpent += 0.1;\n\n  if (resp.error) throw new Error(resp.error_message);\n\n  const chain = resp.data.items.find(c => c.name === chainName);\n  if (!chain) throw new Error(`Chain ${chainName} not found`);\n\n  const lagMs = chain.synced_blocked_signed_at\n    ? Date.now() - new Date(chain.synced_blocked_signed_at).getTime()\n    : Infinity;\n\n  return {\n    isFresh: lagMs < 60_000,      // synced within last 60s\n    lagSeconds: Math.floor(lagMs / 1000),\n    syncedBlock: chain.synced_block_height,\n    totalCreditsSpent,\n  };\n}",
  "buildsWith": [
    "all-chains-status",
    "agent-wallet-skill",
    "agent-fleet-monitor"
  ]
}