{
  "title": "Token Price History",
  "description": "Fetch historical spot prices and OHLCV data for any ERC-20 token onchain.",
  "category": "analytics",
  "url": "https://goldrush.dev",
  "tag": "API Key Required",
  "date-added": "2026-03-12",
  "slug": "token-price-history",
  "complexity": "Beginner",
  "useCase": "Power a price history chart for any ERC-20 token — show daily USD close prices over a custom date range for portfolio analytics and DeFi dashboards.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei",
    "bitcoin"
  ],
  "creditCost": "1 credit/call",
  "endpoints": [
    "GET /v1/pricing/historical_by_addresses_v2/{chainName}/{quoteCurrency}/{contractAddresses}/"
  ],
  "keyParams": {
    "chainName": "EVM chain identifier (e.g. eth-mainnet, base-mainnet)",
    "quoteCurrency": "Quote currency symbol (e.g. USD)",
    "contractAddress": "ERC-20 token contract address"
  },
  "responseFields": [
    "items[].contract_address",
    "items[].prices[].date",
    "items[].prices[].price"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nasync function getTokenPriceHistory(\n  chainName: string,\n  contractAddress: string\n) {\n  const resp = await client.PricingService.getTokenPrices(\n    chainName,\n    'USD',\n    contractAddress\n  );\n\n  if (resp.error) throw new Error(resp.error_message);\n\n  const token = resp.data.items[0];\n  return {\n    address: token.contract_address,\n    prices: (token.prices ?? []).map(p => ({\n      date: p.date,\n      priceUsd: p.price,\n    })),\n  };\n}",
  "buildsWith": [
    "token-price-chart",
    "spot-price-ticker",
    "portfolio-history"
  ]
}