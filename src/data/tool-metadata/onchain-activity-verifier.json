{
  "title": "Onchain Activity Verifier",
  "description": "Verify on-chain activity depth with transaction count, wallet age, chains used, and token diversity â€” build KYC-lite gates, Sybil filters, and proof-of-personhood checks.",
  "category": "identity",
  "url": "https://goldrush.dev",
  "tag": "API Key Required",
  "date-added": "2026-03-16",
  "slug": "onchain-activity-verifier",
  "complexity": "Intermediate",
  "useCase": "Build a Sybil-resistance gate for a DeFi airdrop — score wallets by age, total transactions, and chain diversity to filter out bot accounts and fresh wallets.",
  "chains": [
    "ethereum",
    "base",
    "polygon",
    "bsc",
    "optimism",
    "arbitrum",
    "avalanche",
    "gnosis",
    "fantom",
    "celo",
    "moonbeam",
    "blast",
    "linea",
    "scroll",
    "zkSync",
    "mantle",
    "sonic",
    "unichain",
    "sei",
    "bitcoin"
  ],
  "creditCost": "0.1 credit/call",
  "endpoints": [
    "GET /v1/{chainName}/address/{walletAddress}/transactions_summary/",
    "GET /v1/address/{walletAddress}/activity/"
  ],
  "keyParams": {
    "chainName": "Primary EVM chain to check (e.g. eth-mainnet)",
    "walletAddress": "0x wallet address to verify"
  },
  "responseFields": [
    "items[].total_count",
    "items[].earliest_transaction.block_signed_at",
    "items[].name",
    "items[].last_seen_at"
  ],
  "snippet": "import { GoldRushClient } from '@covalenthq/client-sdk';\n\nconst client = new GoldRushClient(process.env.GOLDRUSH_API_KEY!);\n\nasync function verifyOnchainActivity(chainName: string, walletAddress: string) {\n  const [summaryResp, activityResp] = await Promise.all([\n    client.TransactionService.getTransactionSummary(chainName, walletAddress),\n    client.BaseService.getAddressActivity(walletAddress),\n  ]);\n\n  if (summaryResp.error) throw new Error(summaryResp.error_message);\n  if (activityResp.error) throw new Error(activityResp.error_message);\n\n  const summary = summaryResp.data.items[0];\n  const firstTx = summary?.earliest_transaction?.block_signed_at;\n  const ageInDays = firstTx\n    ? Math.floor((Date.now() - new Date(firstTx).getTime()) / 86_400_000)\n    : 0;\n  const activeChains = activityResp.data.items.filter(c => !c.is_testnet).length;\n\n  const score = Math.min(\n    100,\n    (ageInDays > 365 ? 40 : ageInDays / 365 * 40) +\n    Math.min(40, (summary?.total_count ?? 0) / 10) +\n    Math.min(20, activeChains * 4)\n  );\n\n  return { walletAgeInDays: ageInDays, totalTransactions: summary?.total_count ?? 0, activeChains, score };\n}",
  "buildsWith": [
    "wallet-age-summary",
    "proof-of-onchain-activity",
    "address-activity-scanner"
  ]
}